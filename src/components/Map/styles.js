import styled from "styled-components";

export const MapWrapper = styled.div`
  height: 100%;
  z-index: 0;

  & .marker {
    background: #FA824D;
    height: 4px;
    width: 4px;
    border-radius: 50% ;
    box-shadow: 0 0 5px #f57c00;
    z-index: 0;
  }

`;